/*
    Module: 80-layout-overrides.qss
    Purpose:
    - Compatibility and local override hooks.
    - Keep minimal; use domain modules first.
    Owns:
    - Small aliases needed by legacy property/object names
*/

/* Keep these aliases for compatibility where code still references them. */
QFrame#filterFrame {
    background-color: #eceff2;
    border: none;
}
